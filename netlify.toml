[build]
  command = "pnpm build"

[build.environment]
  NODE_VERSION = "20"

# Netlify auto-detects Next.js and uses the OpenNext adapter.
# Do not set a publish directory — the adapter handles output.
